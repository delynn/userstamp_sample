<h1>Listing posts</h1>

<table>
  <tr>
    <th>Creator</th>
    <th>Updater</th>
    <th>Title</th>
  </tr>

<% for post in @posts %>
  <tr>
    <td><%=h post.creator_id %></td>
    <td><%=h post.updater_id %></td>
    <td><%=h post.title %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New post', new_post_path %>
